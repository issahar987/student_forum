<%= form_with(model: role) do |form| %>
  <% if role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :isEmployee, style: "display: block" %>
    <%= form.check_box :isEmployee %>
  </div>

  <div>
    <%= form.label :isRepairBuisness, style: "display: block" %>
    <%= form.check_box :isRepairBuisness %>
  </div>

  <div>
    <%= form.label :isUser, style: "display: block" %>
    <%= form.check_box :isUser %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
