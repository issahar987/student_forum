<!DOCTYPE html>
<html>
  <head>
    <title>Help Desk Forum</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
      <ul>
         <li><%= link_to "Strona główna", root_path %></li>
        <% if logged_in? %>
          Zalogowany jako <%= current_user.login %>
          <%= link_to "Profil", current_user, style: "display: block" %>
          <%= link_to "Wyloguj", logout_path, method: :delete %>
          <%= link_to "Posty", forum_posts_path, style: "display: block" %>
          <%= link_to "Użytkownicy", users_path, style: "display: block" %>
        <% else %>
          <%= link_to "Zaloguj", login_path %>
        <% end %>
        <%= yield %>
        </ul>
  </body>
</html>
